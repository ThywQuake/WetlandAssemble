# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D002 | M001 discussion | pattern | How M001 will classify the state of phases, modules, and workflows | Assess project status with evidence grades: validated, implemented-but-unverified, historical/stale path, or unclear. | The current risk is not just incomplete work; it is conflation of local tests, stash notes, old routes, and real proof. Evidence grading makes those proof levels explicit. | No | collaborative |
| D003 | M001 discussion | scope | What breadth of repository coverage M001 should audit | Scan the whole project evenly rather than only the latest Phase 4 or current-branch surfaces. | The user explicitly wants to understand what has been done and what comes next across the entire workspace, not only the newest line of work. | Yes — future milestones can narrow focus after M001 establishes the baseline. | collaborative |
| D004 | M001 discussion | convention | What change boundary M001 is allowed to cross | Keep M001 analysis-first: do not continue business/scientific implementation or broad refactors during the audit milestone. | Mixing new implementation or broad cleanup into the audit would distort the evidence surface while it is being established. | Yes — after M001, implementation and cleanup can resume from the audited route. | collaborative |
| D005 | M001/S01 | requirement | R001 | validated | M001/S01 produced `.gsd/milestones/M001/slices/S01/S01-INVENTORY.md` plus `.gsd/milestones/M001/slices/S01/S01-DRIFT-BOUNDARIES.md`, covering code, scripts, tests, plans, stashes, dataset docs, TODOs, branch/worktree state, and explicit absent-local / external-HPC proof boundaries. All slice verification checks passed, and `python -m pytest --collect-only -q` collected 418 tests successfully. | Yes | agent |
| D006 | M001/S01 | audit | How to separate the S01 evidence freeze from route interpretation | Keep `S01-INVENTORY.md` as the frozen replayable fact base and place current-vs-historical interpretation in `S01-DRIFT-BOUNDARIES.md`. | S02 and S03 need a stable inventory they can cite without recomputing counts or mixing raw facts with route judgments. Separating the appendix preserves a canonical evidence snapshot while still documenting drift classification. | Yes | agent |
| D007 | M001/S01 | evidence-model | How to interpret missing `results/`, `temp/`, and unreachable memory surfaces inside the M001 GSD worktree | Treat them as `absent-local` proof boundaries, not as evidence that the artifacts or history never existed. | The audit worktree excludes ignored outputs and cannot reach some historical memory/HPC paths. Treating absence as nonexistence would create false negatives and mislead later slices about remote or ignored-state proof. | Yes | agent |
| D008 | M001/S02 planning | audit-artifact | How S02 will publish the evidence-graded state snapshot for phases and modules | Use one canonical `.gsd/milestones/M001/slices/S02/S02-PHASE-MODULE-MATRIX.md` artifact with separate Phase Matrix and Module Matrix sections, and require every row to carry a D002 grade plus `Local evidence`, `HPC / external proof`, and `Why this grade` fields. | A single reader-first matrix lets downstream slices consume stable evidence without re-auditing the repository, while keeping proof boundaries explicit and preserving the critical current-vs-historical Phase 4 split for later route judgment. | Yes | agent |
| D009 | M001/S02/T02 | route-judgment | How to classify the divergent Phase 4 paths in the S02 evidence matrix | Keep the 2026-04-06 Stage-1 / Stage-2 regional chain as the current Phase 4 route and classify the 2026-04-05 full-tropics reducer chain as a separate historical/stale path row. | The newer 2026-04-06 changelog entries plus the Phase 4 conversation summary and recall note explicitly supersede the older full-tropics reducer route after repeated HPC OOM problems. Merging the two paths into one Phase 4 status would hide the real re-entry point and blur route-history ambiguity with current execution guidance. | Yes | agent |
| D010 | M001/S02/T03 | evidence-model | How to grade module families that span multiple phases in the S02 matrix | Grade each module family against the phase that actually proves that implementation surface instead of automatically downgrading validated core modules because later continuation phases remain unverified. | The S02 matrix needs phase rows and module rows to stay consistent under D002. This keeps validated cores like `fine-grained comparison` anchored to Phase 3 proof while later Phase 3.6/3.7 runtime and presentation paths remain `implemented-but-unverified`. | Yes | agent |
| D011 | M001/S02/T04 | evidence-model | How to grade visualization surfaces in the S02 matrix | Keep cross-phase visualization families at `implemented-but-unverified` unless fresh end-to-end outputs, external imagery inputs, and the current runtime path are all proven together. | Local plotting tests prove renderer existence and file-writing behavior, but they do not prove that current scientific outputs were regenerated from real Phase 2.6/3.6/3.7/4 artifacts on the live recommended route. The matrix needs that distinction to avoid overstating proof. | Yes | agent |
| D012 | M001/S02 | requirement | R002 | validated | M001/S02 completed `.gsd/milestones/M001/slices/S02/S02-PHASE-MODULE-MATRIX.md`, which restates the D002 grading contract, covers the full phase and module matrix, and cites concrete source, test, changelog, and stash evidence for each row. | Yes | agent |
| D013 | M001/S02 | requirement | R007 | validated | M001/S02 completed `.gsd/milestones/M001/slices/S02/S02-PHASE-MODULE-MATRIX.md`, which separates `Local evidence` from `HPC / external proof` for every phase and module row and closes with `Requirement Coverage` plus `Open Proof Gaps`. | Yes | agent |
| D014 | M001/S03 planning | audit-artifact | How S03 will publish current-vs-stale Phase 4 route judgment and risk handoff | Use one canonical `.gsd` route-audit artifact for route judgment and risk handoff, and keep the Chinese stash note as a compact pointer back to that canonical doc. | S03 needs to stop route ambiguity, not create another narrative surface. Keeping route classification, stale-route demotion, and the risk register in one canonical `.gsd/milestones/M001/slices/S03/S03-ROUTE-AUDIT-RISK-REGISTER.md` artifact lets downstream slices cite a single source of truth, while the stash note remains a lightweight recovery entrypoint for operators. | Yes | agent |
| D015 | M001/S03 | requirement | R003 | validated | M001/S03 published `.gsd/milestones/M001/slices/S03/S03-ROUTE-AUDIT-RISK-REGISTER.md`, which names the authoritative Phase 4 continuation chain as Stage-1 pixel-stats build/submit feeding `scripts/run_phase4_regional.py`, and explicitly notes that no repo-local broad `scripts/run_phase4_trend_analysis.py` mainline exists. | Yes | agent |
| D016 | M001/S03 | requirement | R004 | validated | M001/S03's canonical route audit explicitly lists the stale or misleading routes — the full-tropics shard/reduce family, older `_staging`-as-mainline wording, and the missing planned batch runner — with evidence anchors and concrete misread risks. | Yes | agent |
| D017 | M001/S03 | requirement | R005 | validated | M001/S03's `Risk Register` records the open stage-numbering drift, changelog self-conflict, HPC-only Stage-1/Stage-2 proof gap, GWD30 input divergence, and stale-plan/test weight, making uncertainty explicit without claiming remote proof is closed. | Yes | agent |
| D018 | M001/S03 | route-judgment | Which Phase 4 continuation path is authoritative after S03 | Treat `scripts/build_phase4_gwd30_pixel_stats.py` / `scripts/submit_phase4_gwd30_pixel_stats.sh` feeding `scripts/run_phase4_regional.py` as the current mainline; keep `scripts/hpc_probe_trends.py` supporting-only; do not assume a repo-local broad `scripts/run_phase4_trend_analysis.py` runner exists. | The S02 matrix, current source code, and 2026-04-06 changelog/stash reset evidence agree that the Stage-1 pixel-stats plus Stage-2 regional route replaced the earlier full-tropics reducer lane after HPC OOM issues, while the trend probe remains diagnostic rather than a full continuation path. | Yes | agent |
| D019 | M001/S04 planning | execution-map | How S04 will define the post-audit continuation ladder for fresh Phase 4 proof | Freeze a narrow-first execution ladder: read the canonical S03 route audit first, confirm the S02 Phase 4/Open Proof Gaps context second, then run a 2016 direct Stage-1 GWD30 pixel-stats build, a matching 2016 amazon Stage-2 regional proof, widen only after those pass, and keep any compact Chinese stash note strictly subordinate to the canonical S04 execution map. | The live Stage-1 builder, submit wrapper, and Stage-2 regional runner all widen silently when year/dataset/region filters are omitted, the project default recovery year is 2016, and the open uncertainty is fresh HPC proof rather than local route ambiguity. Freezing a narrow-first ladder minimizes operator error, keeps stale flags/routes from regaining weight, and gives S05 one canonical execution source to package later. | Yes | agent |
| D020 | M001/S04 | requirement | R006 | validated | M001/S04 produced `.gsd/milestones/M001/slices/S04/S04-NEXT-STEP-EXECUTION-MAP.md`, which consolidates the canonical read order, the narrow-first `2016 -> amazon` continuation ladder, the exact proof targets, and the avoid-first guardrails into one execution artifact while explicitly keeping the inherited HPC-only rerun gap open. | Yes | agent |
| D021 | M001/S05 planning | continuity | How S05 will package milestone re-entry without creating new competing sources of truth | Use one canonical S05 operator recovery pack as the first-stop index, while keeping S03 authoritative for route truth, S04 authoritative for execution order, and any new stash breadcrumb strictly subordinate to those canonical artifacts. | S05 owns R008 and is a packaging slice, not a fifth audit. Freezing the hierarchy now prevents future operators from re-promoting older recall notes or compact breadcrumbs into alternate route/execution sources. | Yes | agent |
| D022 | M001/S05/T01 | documentation | Operator recovery source precedence for Milestone M001 | Use S05 as the first-stop recovery index, S03 as route truth, S04 as execution truth, S02 as proof-boundary/status grading, and S01 as raw evidence; subordinate older stash re-entry notes to that chain. | The repo now contains multiple high-signal recovery artifacts. Freezing a precedence order prevents future re-entry from recreating competing summaries or treating older stash breadcrumbs as equal to the canonical route and execution documents. | Yes | agent |
| D023 | M001/S05 | requirement | R008 | validated | M001/S05 completed `.gsd/milestones/M001/slices/S05/S05-OPERATOR-RECOVERY-PACK.md` plus `docs/stashes/2026-04-07-009-m001-s05-operator-recovery-pack-reentry.md`, giving future operators one compact first-stop recovery index that routes route-truth questions back to S03, execution copying back to S04, and keeps the inherited HPC-only proof gap explicit. | Yes | agent |
| D001 | M001 discussion | scope | How to structure the first milestone in this messy workspace | Use M001 as a standalone analysis milestone dedicated to reconstructing authoritative project state before further implementation continues. | The workspace already contains substantial code, results, tests, plans, and route pivots. Continuing feature work before re-establishing current state would amplify confusion and make later choices low-trust. | Yes — once M001 completes and the project state is authoritative. | collaborative |
| D024 | M002 discussion | roadmap | How to sequence the post-audit thesis implementation work | Use three sequenced milestones: M002 for unified evidence contract and core outputs, M003 for hotspot-cause explanation, and M004 for fraction-first fusion plus evaluation. | The current repository already contains comparison and trend primitives, but the project still lacks one unified paper-aligned contract, a stable explanation framework, and a defensible fusion scorecard. Sequencing contract -> explanation -> fusion reduces scope drift and keeps later milestones from redefining core outputs. | Yes — if execution proves one milestone boundary is too coarse or too fine. | collaborative |
| D025 | M002 discussion | evidence | What evidence leg should be mandatory for hotspot-cause interpretation | Treat hotspot-cause explanation as quantitative-evidence-first; keep imagery as supporting evidence rather than the mandatory proof leg. | The thesis needs reproducible, reviewable explanations for disagreement hotspots. Quantitative auxiliary evidence can support hypothesis testing and cross-hotspot comparison more directly than imagery-first case studies, which remain useful but secondary. | Yes — if early hotspot packages show the quantitative evidence is insufficient without stronger imagery proof. | collaborative |
| D026 | M002 discussion | integration | Which land-cover context source to standardize for hotspot-cause analysis | Require MODIS MCD12Q1 through GEE as the land-cover evidence source for hotspot interpretation. | The project needs a consistent, externally sourced land-cover layer to interpret hotspot contexts such as forested wetlands, land-cover transitions, and surface-type differences. MCD12Q1 is already available through the existing GEE workflow surface and fits the later explanation milestone. | Yes — if implementation proves a second land-cover product is needed for critical gaps. | collaborative |
| D027 | M002 discussion | proof | How to order proof for the first implementation milestone | Prove the unified pipeline on a hydro-diverse canonical subset first, then scale the same contract to all ten regions within M002. | Subset-first proof lowers operational and schema risk without shrinking the final thesis scope. Choosing the subset by hydro-setting diversity ensures the first proof retires mechanism coverage risk instead of only regional coverage risk. | No | collaborative |
| D028 | M002 discussion | fusion | How to define the first fusion target and its evaluation logic | Make the first fusion target a fraction-first product evaluated with a balanced multi-objective scorecard spanning external consistency, disagreement reduction, and trend realism. | A fraction-first target keeps the first fused product tractable and comparable to existing percentage/trend evidence lines. A balanced multi-objective scorecard avoids reducing fusion quality to a single metric and preserves the thesis' three-way evidence logic. | Yes — after the first scorecard iteration exposes missing evaluation dimensions or a need for a fused classification product. | collaborative |
| D029 | M002/S01/T04 | architecture | How the canonical percentage proof path should orchestrate summaries, surfaces, and hotspots | Use a thin Phase 4 percentage contract runner that composes the existing Phase 4 regional producer and the existing 0.25° surface builder, then writes contract-stable hotspot manifests and figures. | This keeps the S01 proof path on the live cache-backed producers, preserves visible stage logging and cache reuse, and avoids inventing a second standalone wetland-percentage pipeline just to add hotspot manifests. | Yes | agent |
| D030 | M002/S01/T02 | architecture | Where the shared 0.25° percentage surface logic should live | Make `src/WA/comparison/percentage_backbone.py` the single owner of the surface/cache/region-resolution path and keep `scripts/plot_tropical_wetland_025deg.py` as a thin wrapper. | This prevents the plotting script and downstream orchestration CLIs from drifting into separate implementations, lets the hotspot runner import one stable surface API directly, and keeps GWD30 Stage-1 support in the shared backbone instead of in a one-off script branch. | Yes | agent |
| D031 | M002/S02 planning | architecture | How should trend and trend-agreement artifacts be named on the shared evidence contract | Extend `EvidenceContract` with dedicated trend/trend-agreement artifact families and key agreement outputs by a deterministic participant-set id derived from sorted dataset ids. | This preserves the S01 percentage stems, gives `TrendResult` / `TrendAgreementResult` stable non-overwriting relpaths, and lets later slices reload agreement outputs without inferring participant membership from ad hoc filenames. | Yes | agent |
| D032 | M002/S02/T01 | naming | Trend agreement participant-set key delimiter | Use sorted dataset ids joined with '+' for trend agreement participant_set_key values, while leaving '__' reserved for the evidence-contract stem separator. | Agreement artifacts need a deterministic participant-set id from sorted dataset ids, but reusing '__' inside that key would blur the existing '<dataset_or_key>__<region>__<suffix>' file-stem structure. Joining the sorted ids with '+' keeps filenames stable and readable for later reload helpers. | Yes | agent |
| D033 | M002/S02/T02 | phase4-trends | Default/allowed dataset set for the trend-contract runner | Limit the trend-contract runner to gwd30, giems_mc, swamps, and wad2m until additional dataset loaders have explicit trend-proof coverage. | The trend-contract runner should only orchestrate dataset ids that the existing trend loaders/tests already prove trend-capable, instead of inheriting the broader percentage-runner list and silently assuming parity for auxiliary or less-proven datasets such as berkeley_rwawc or topmodel. | Yes | agent |
| D034 | M002/S03 planning | architecture | How classification-disagreement outputs should join the shared evidence contract | Use a thin `classification_contract.py` adapter plus `run_phase4_classification_contract.py` runner that wrap `phase36.py` and `phase37_hotspots.py` outputs instead of moving contract logic into those producer modules. | This preserves the proven cache-backed global disagreement backbone, keeps hotspot-selection rules in one place, retains visible stage logging/resume behavior, and gives later slices one contract-layer surface to consume without reverse-engineering legacy phase3.6/3.7 filenames. | Yes | agent |
| D035 | M002/S03 planning | naming | How to encode the classification participant set in contract filenames and metadata | Use sorted dataset ids joined with `+` as the classification `participant_set_key` (for this slice: `g2017+glwd_v2+gwd30`). | This matches the trend-agreement naming convention, keeps the outer `<dataset_or_key>__<region>__<suffix>` stem unambiguous, and avoids inventing a second key grammar before S04 unifies the hotspot ledger across percentage, trend, and classification lines. | Yes | agent |
| D036 | M002/S03/T01 | naming | How the shared evidence contract should protect its '<dataset_or_key>__<region>__<suffix>' filename grammar when participant-set keys are used in the dataset slot | Validate shared evidence-contract stem tokens by trimming whitespace and rejecting '__' or path separators inside dataset/participant keys before relpaths or metadata are emitted. | Both trend-agreement and classification artifacts use participant-set keys in the dataset slot. Enforcing '__' as a reserved outer separator at the contract layer prevents ambiguous stems, relpath collisions, and downstream reload helpers from having to guess whether a double underscore belongs to the key or the contract grammar. | Yes | agent |
| D037 | M002/S04 planning | evidence | How trend hotspots should be scored and selected on the shared contract | Use disputed cells from trend-agreement outputs as the trend-hotspot candidate mask and rank them by `disagreement_score = 1 - agreement_ratio`, with `slope_std` as the first tie-breaker. | S04 needs trend hotspots that represent trend-correctness disagreement, not raw trend intensity. Disagreement-first scoring stays aligned with the S02 agreement semantics, reuses existing `disputed` / `agreement_ratio` / `slope_std` fields, and avoids conflating large absolute slopes with cross-dataset disagreement. | Yes | agent |
| D038 | M002/S04 planning | integration | How cross-line hotspot comparison should be represented on disk and in downstream reload helpers | Add one long-form `unified_hotspot_ledger` artifact family keyed by a stable `analysis_object_id`, preserve family-local score fields/units plus provenance paths in each row, and make the ledger runner fail closed unless percentage, classification, and trend hotspot families are all present and semantically valid for the region. | One ledger artifact gives S04/S05/S06 a deterministic cross-line analysis object without inventing fake score comparability or proliferating multiple derived artifact families. Fail-closed completeness checks prevent downstream comparison surfaces from silently mixing partial hotspot coverage across the three evidence lines. | Yes | agent |
| D039 | M002/S04 planning | integration | How cross-line hotspot comparison should be represented on disk and in downstream reload helpers | Add one long-form `unified_hotspot_ledger` artifact family keyed by a stable `analysis_object_id`, preserve family-local score fields/units plus provenance paths in each row, and make the ledger runner fail closed unless percentage, classification, and trend hotspot families are all present and semantically valid for the region. | One ledger artifact gives S04/S05/S06 a deterministic cross-line analysis object without inventing fake score comparability or proliferating multiple derived artifact families. Fail-closed completeness checks prevent downstream comparison surfaces from silently mixing partial hotspot coverage across the three evidence lines. | Yes | agent |
| D040 | M002/S04/T02 | architecture | How the unified hotspot ledger represents heterogeneous hotspot scores | Store one row per analysis object with metric_family, primary_score_name, primary_score_value, family_percentile, and line_specific_json instead of collapsing percentage, entropy, and disagreement into a fake shared raw score. | Percentage coverage, classification entropy, and trend disagreement are not numerically comparable across families. Keeping the family-local score name/value plus a within-family percentile preserves ranking semantics while still giving downstream slices one stable analysis_object_id ledger surface with provenance. | Yes | agent |
| D041 | M002/S04 | artifact-integrity | How contract-backed hotspot manifests and ledgers should be reused | Only treat hotspot JSON/CSV pairs and unified ledgers as reusable when both files are present and the manifest metadata plus table SHA-256 validate; otherwise fail closed instead of silently reusing partial artifacts. | M002/S04 added trend hotspot manifests and the unified hotspot ledger as shared downstream evidence objects. S05/S06 will depend on skip/rebuild behavior during wider reruns, so partial or mixed-region files must be rejected before they can be mistaken for valid cached evidence. | Yes | agent |
| D042 | M002/S05 planning | roadmap | How S05 will handle repo-reality drift before ten-region scale-out | Treat restoration of the missing percentage/classification producer chain plus a contract-level ordered ten-region selector as required S05 work before any ten-region automation, and keep the unified ledger as the final readiness gate rather than the generator. | The current snapshot lacks the percentage and classification producer files claimed by earlier slice artifacts, `run_phase4_regional.py` still defaults to macro+priority regions, and the ledger can only reopen complete hotspot families. Without first restoring real producers and freezing one shared ten-region selector in code, any claimed ten-region proof would remain operationally false and would not satisfy R107. | Yes | agent |
| D043 | M002/S05/T01 | architecture | How Phase 4 CLIs should expose the shared ten-region selector without breaking existing regional defaults | Make EvidenceContract the single owner of the ordered `ten` subset, require contract-aware CLIs to use explicit `--subset {canonical,ten}` or `--region` selection with ambiguity rejection, and keep `run_phase4_regional.py`'s no-arg macro+priority run as an explicit legacy path instead of silently redefining it to ten regions. | Downstream scale-out work needs one reproducible ten-region source that is not hand-written per script, but the existing regional runner already has operational macro-region behavior that other routes may still rely on. Preserving the legacy no-arg path while forcing explicit contract subsets for wide runs makes the migration visible, keeps selector failures fail-closed, and exposes the resolved ordered region list in logs before fanout. | Yes | agent |
| D044 | M002/S05/T02 | architecture | How the restored percentage contract family should encode multi-dataset surfaces and hotspot scoring after GWD30 is brought back into the shared 0.25° path | Treat `dataset_key=canonical` as the ordered six-dataset percentage bundle (`gwd30`, `giems_mc`, `topmodel`, `swamps`, `wad2m`, `berkeley_rwawc`), write one contract `surface` artifact that stacks the per-dataset coarse surfaces plus derived mean/std/count metrics, and rank percentage hotspots by the bundle’s `mean_wetland_percentage` rather than by per-dataset files. | S05 needs one semantically reloadable percentage family per region that the ledger and readiness steps can trust. A stacked contract surface keeps the live per-dataset producers visible, restores GWD30 without inventing a parallel format, and lets hotspot rows keep `wetland_percentage` as a real family-local score while still carrying enough provenance for downstream comparison. | Yes | agent |
| D045 | M002/S05/T03 | architecture | Phase 4 classification contract family identity and hotspot provenance | Keep `classification_key=canonical` as the contract family key for the fixed `g2017+glwd_v2+gwd30` participant set, and rewrite region hotspot artifacts from the Phase 3.7 source trio instead of recomputing hotspot selection inside the Phase 4 adapter. | This preserves compatibility with the existing hotspot-ledger contract (`classification_key=canonical`), avoids duplicating Phase 3.6/3.7 disagreement science in a second code path, and lets the adapter fail closed by cross-checking Phase 3.6 source paths plus Phase 3.7 manifest/CSV/region-summary consistency before any contract hotspot pair is trusted. | Yes | agent |
| D046 | M002/S05/T04 | architecture | How Phase 4 trend reruns separate downstream contract artifacts from resumable compute checkpoints | Keep contract trend_surface/trend_regional_summary artifacts keyed by dataset_id + region_id, and store resumable trend checkpoints as separate region/dataset/aggregation/requested-window cache files consumed before agreement. | Downstream consumers need stable semantic artifact paths that do not change with every rerun, while HPC reruns need explicit window-scoped checkpoints that can resume safely without colliding with different time ranges or silently reusing stale compute state. Separating the two lets the runner validate checkpoint metadata strictly, reuse dataset-scoped compute before agreement, and still publish stable contract outputs for later ledger/readiness work. | Yes | agent |
| D047 | M002/S05/T05 | observability | How the Phase 4 scale-out readiness gate classifies incomplete hotspot families and supports ledger failures | Treat a hotspot family as 'missing' only when both the manifest and CSV are absent; treat partial pairs or semantic/provenance reload failures as 'partial', and auto-write a single-region readiness report when the ledger CLI fails. | Operators need deterministic ready/missing/partial states that do not over-report incompleteness as readiness. Distinguishing absent families from corrupt or half-written families makes ten-region reruns diagnosable, while automatic per-region readiness reports preserve fail-closed ledger behavior without forcing operators to reconstruct the failing family context by hand. | Yes | agent |
| D048 | M002/S06 planning | architecture | How S06 should package paper-facing outputs and decide whether a ten-region pack is complete | Keep paper-facing outputs as a derived layer under a dedicated pack root (for example `results/figures/phase4_pack`) that only reopens contract artifacts semantically, and require readiness plus unified-ledger success across the requested regions before `--strict` mode can claim a complete ten-region pack. | S01-S05 already established `results/phase4` as the science contract tree plus readiness/ledger gates. Mixing paper-facing figures and summaries back into that tree would blur stable science artifacts with derived presentation products. Reopening contract surfaces semantically preserves provenance, keeps `EvidenceContract` and the unified ledger as the only owners of filenames and ordering, and lets the paper pack stay replayable. Requiring readiness + ledger before strict success also prevents local smoke outputs or partial ten-region reruns from being mistaken for milestone-level completion, which is necessary to satisfy R113 without weakening R107. | Yes | agent |
| D049 | M002/S06/T02 | architecture | Phase 4 paper-pack output shape | Use one wide joined regional evidence CSV plus one long-form unified hotspot CSV, keep exact source artifact paths in both the joined table and manifest, and clear any stale manifest before a rebuild so only a fully successful pack can claim completeness. | The joined table gives paper-facing one-row-per-region evidence across percentage/classification/trend/ledger, while the hotspot table preserves the existing long-form ledger detail without inventing a second hotspot schema. Duplicating provenance into both the joined table and manifest keeps reruns replayable and inspectable, and removing stale manifests prevents partial reruns from leaving a misleading complete-pack claim behind. | Yes | agent |
| D050 | M002/S06/T03 | observability | How the Phase 4 evidence-pack CLI should expose complete-pack proof outcomes | Always write deterministic proof JSON/Markdown artifacts under the pack root; only `--strict` turns an incomplete readiness/ledger proof into a non-zero exit, while non-strict runs return success with explicit incomplete-proof artifacts and no fresh manifest claim. | The pack CLI is now the single complete-pack claim surface, so every run needs a replayable proof artifact whether it succeeds or not. Separating proof writing from strict exit behavior lets operators inspect readiness or ledger gaps without mistaking an incomplete dry run for a complete pack, and clearing the stale manifest up front prevents old success claims from surviving a failed proof attempt. | Yes | agent |
