# 2026-04-09 M002/S06/T01 Quick Reference

## What shipped

- `src/WA/comparison/trend_contract.py`
  - Added public `trend_agreement_*_output_path(...)` helpers.
  - Added public semantic reload helpers for trend agreement surface/summary plus pair-level `load_contract_trend_agreement_artifacts(...)`.
  - Tightened validation for partial pairs, malformed metadata, mixed participant ids, and overlap-window mismatches with `stage=agreement region_id=... participant_set_key=...` context.
- `scripts/run_phase4_trend_contract.py`
  - Reused the public trend-agreement output/reload helpers instead of script-private filename/path logic.
- `src/WA/visualization/phase4.py`
  - Added pack-facing wrappers for percentage summary/surface and trend-agreement summary/surface.
  - Kept wrapper errors aligned with the existing `Phase4 semantic reload failed ...` style.
- Tests
  - `tests/test_comparison/test_trend_contract.py` now covers semantic reopen of trend agreement artifacts plus negative cases.
  - `tests/test_visualization/test_phase4.py` now covers the new percentage/trend pack-facing wrappers.

## Verification

### Task-level

- `ruff check src/WA/comparison/trend_contract.py scripts/run_phase4_trend_contract.py src/WA/comparison/percentage_backbone.py src/WA/visualization/phase4.py tests/test_comparison/test_trend_contract.py tests/test_visualization/test_phase4.py` ✅
- `python scripts/run_phase4_trend_contract.py --help` ✅
- `python -m pytest tests/test_comparison/test_trend_contract.py tests/test_visualization/test_phase4.py -q` ✅ (`22 passed`)

### Slice/project context

- `python scripts/run_related_tests.py src/WA/comparison/trend_contract.py src/WA/visualization/phase4.py src/WA/visualization/phase4_pack.py scripts/run_phase4_evidence_pack.py src/WA/test_selection.py` ✅ (selector only; prints recommended pytest subset)
- Slice-level pack checks still fail **as expected for T01** because T02/T03 files do not exist yet:
  - `src/WA/visualization/phase4_pack.py`
  - `scripts/run_phase4_evidence_pack.py`
  - `tests/test_visualization/test_phase4_pack.py`
- `python -m pytest tests/` did **not** close green repo-wide status; it surfaced the known baseline red `tests/test_mgrs_tiling.py::test_tile_to_extent_matches_reference_case` before the process later exited `137`.

## Open risks / TODOs

- T02 still needs to create the derived pack module/CLI and its tests before the slice-level pack verification can pass.
- T03 still needs strict readiness/ledger proof gating.
- Baseline repo issue remains: `tests/test_mgrs_tiling.py::test_tile_to_extent_matches_reference_case` fails on exact float equality and is unrelated to this task.

## HPC commands after rsync

No new science rerun is required for T01 itself; this was a pack-safe API task. After syncing to HPC, the useful smoke checks are:

```bash
python -m pytest tests/test_comparison/test_trend_contract.py tests/test_visualization/test_phase4.py -q
python scripts/run_phase4_trend_contract.py --help
```

For the eventual S06 end-to-end ladder, continue to use the planned sequence: percentage -> classification -> trend -> readiness -> ledger -> pack `--strict`.
