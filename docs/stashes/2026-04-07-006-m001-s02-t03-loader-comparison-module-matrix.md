# M001 S02 T03 — loader/comparison module matrix block

- Added the first `## Module Matrix` block to `.gsd/milestones/M001/slices/S02/S02-PHASE-MODULE-MATRIX.md`.
- Classified five module families with the same D002 grade vocabulary and the same `Local evidence` vs `HPC / external proof` split already used by the phase rows.
- Added concrete source/test anchors for:
  - `loaders/classification`
  - `standardized loader`
  - `standardization & GWD30 staging`
  - `rough comparison`
  - `fine-grained comparison`
- Kept the standardized-input families at `implemented-but-unverified` because they still inherit Phase 1.1 / 1.5 / 3.6 HPC proof gaps.
- Marked the rough/fine comparison cores as `validated` because their rows inherit Phase 2 / Phase 3's recorded local + HPC-backed evidence rather than the later visualization / presentation gaps.
