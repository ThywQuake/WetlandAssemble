# 2026-04-09 M002/S05 Slice Closeout Quick Reference

## 本 slice 实际交付 / What actually shipped

S05 没有假装“十区结果已经在本地全量生成完毕”；它真正闭合的是 **十区 scale-out 的合同与可复跑执行面**：

- `EvidenceContract` 现在是唯一的 ordered `ten` selector owner，`canonical` / `ten` / explicit `--region` 的边界固定且会拒绝歧义输入。
- percentage 线恢复为真实 contract producer：
  - `src/WA/comparison/percentage_backbone.py`
  - `src/WA/comparison/percentage_hotspots.py`
  - `scripts/run_phase4_percentage_contract.py`
  - GWD30 已重新接入 shared `0.25°` path（经 Stage-1 pixel-statistics manifest 恢复）
- classification 线恢复为真实 contract adapter / runner：
  - `src/WA/comparison/classification_contract.py`
  - `scripts/run_phase4_classification_contract.py`
  - `src/WA/visualization/phase4.py` classification semantic reload helpers
- trend 线恢复 dataset-scoped contract outputs，并新增 resumable checkpoints 与 HPC submit wrapper：
  - `src/WA/comparison/trend_contract.py`
  - `src/WA/comparison/trends.py`
  - `scripts/run_phase4_trend_contract.py`
  - `scripts/submit_phase4_trend_contract.sh`
- ten-region readiness gate 已落地：
  - `src/WA/comparison/scaleout_readiness.py`
  - `scripts/run_phase4_scaleout_readiness.py`
  - `scripts/run_phase4_hotspot_ledger.py` 继续 fail-closed，但现在失败时会自动写 readiness diagnostics

## 本地验证结论

### Slice-level verification

- `ruff check ...`（S05 相关 contract / runner / readiness / tests 全集）✅
- `bash -n scripts/submit_phase4_gwd30_pixel_stats.sh scripts/submit_phase4_gwd30_regional_year_split.sh scripts/submit_phase4_gwd30_tropical_shards.sh scripts/submit_phase4_trend_contract.sh` ✅
- `python scripts/run_phase4_regional.py --help` ✅
- `python scripts/run_phase4_percentage_contract.py --help` ✅
- `python scripts/run_phase4_classification_contract.py --help` ✅
- `python scripts/run_phase4_trend_contract.py --help` ✅
- `python scripts/run_phase4_hotspot_ledger.py --help` ✅
- `python scripts/run_phase4_scaleout_readiness.py --help` ✅
- Focused/broader slice pytest subset:

```bash
python -m pytest \
  tests/test_comparison/test_evidence_contract.py \
  tests/test_comparison/test_phase4_regional.py \
  tests/test_comparison/test_percentage_backbone.py \
  tests/test_comparison/test_percentage_hotspots.py \
  tests/test_plot_tropical_wetland_025deg.py \
  tests/test_comparison/test_classification_contract.py \
  tests/test_visualization/test_phase4.py \
  tests/test_comparison/test_trend_contract.py \
  tests/test_comparison/test_trends.py \
  tests/test_comparison/test_trend_agreement.py \
  tests/test_submit_phase4_trend_contract.py \
  tests/test_comparison/test_scaleout_readiness.py \
  tests/test_comparison/test_hotspot_ledger.py \
  tests/test_submit_phase4_gwd30_pixel_stats.py \
  tests/test_submit_phase4_gwd30_regional_year_split.py \
  tests/test_submit_phase4_gwd30_tropical_shards.py \
  tests/test_test_selection.py -q
```

结果：`124 passed in 22.71s` ✅

### Observability / fail-closed smoke

- `python scripts/run_phase4_scaleout_readiness.py --region amazon ...` 在当前空产物目录下成功输出 CSV/JSON readiness report，并把三条 family 全部标成 `missing`，带具体 artifact paths。✅
- `python scripts/run_phase4_hotspot_ledger.py --region amazon ... --no-skip` 在 family 缺失时保持 fail-closed，同时自动写出同一个 single-region readiness diagnostic report，并在日志里打印 percentage / classification / trend 三条 family 的 status/path/reason。✅

## 仍然开放的边界

- 本地 worktree **没有** materialize 十区真实 science outputs；S05 交付的是可复跑执行面、diagnostic gate、以及 contract-safe runners。
- 下一步 S06 仍需在 HPC 上实际生成/复用十区产物，并做 paper-ready pack 与集成验证。
- task summaries 里反复出现的 repo-wide 既有红条（`tests/test_mgrs_tiling.py::test_tile_to_extent_matches_reference_case`）仍是独立问题，不属于本 slice 的 focused regression。

## 现在在 HPC 上该怎么跑（按推荐顺序）

### 1) 先重建 percentage 十区产物

```bash
python scripts/run_phase4_percentage_contract.py \
  --subset ten \
  --output-root results/phase4 \
  --standardized-dir /lustre/home/2200013429/Wetland_Assemble/data/standardized \
  --surface-year 2016 \
  --start-year 2016 \
  --end-year 2016 \
  --no-skip
```

### 2) 再重建 classification 十区产物

```bash
python scripts/run_phase4_classification_contract.py \
  --subset ten \
  --standardized-dir /lustre/home/2200013429/Wetland_Assemble/data/standardized \
  --output-root results/phase4 \
  --year 2016 \
  --phase36-output-dir results/phase3.6 \
  --phase36-cache-dir results/cache/phase3_6 \
  --phase37-output-dir results/phase3.7_hotspots \
  --phase37-cache-dir results/cache/phase3_7 \
  --no-skip
```

### 3) 用 submit wrapper fan out ten-region trend rerun

```bash
bash scripts/submit_phase4_trend_contract.sh \
  --repo "$HOME/repos/WA" \
  --python-bin "$HOME/repos/WA/.venv/bin/python" \
  --standardized-dir /lustre/home/2200013429/Wetland_Assemble/data/standardized \
  --output-root results/phase4 \
  --subset ten \
  --dataset-id gwd30 \
  --dataset-id giems_mc \
  --dataset-id topmodel \
  --dataset-id swamps \
  --dataset-id wad2m \
  --aggregation annual \
  --start-year 1990 \
  --end-year 2020 \
  --min-observations 5 \
  --min-overlap-years 5 \
  --top-hotspots 10 \
  --cpus 2 \
  --time 480 \
  --partition C064M0256G \
  --no-progress
```

### 4) 先扫 readiness，再跑 unified ledger final gate

```bash
python scripts/run_phase4_scaleout_readiness.py \
  --subset ten \
  --output-root results/phase4 \
  --percentage-key canonical \
  --classification-key canonical \
  --trend-dataset-id gwd30 \
  --trend-dataset-id giems_mc \
  --trend-dataset-id topmodel \
  --trend-dataset-id swamps \
  --trend-dataset-id wad2m
```

```bash
python scripts/run_phase4_hotspot_ledger.py \
  --subset ten \
  --output-root results/phase4 \
  --ledger-key canonical \
  --percentage-key canonical \
  --classification-key canonical \
  --trend-dataset-id gwd30 \
  --trend-dataset-id giems_mc \
  --trend-dataset-id topmodel \
  --trend-dataset-id swamps \
  --trend-dataset-id wad2m \
  --no-skip
```

## 给 S06 的一句话

S05 交付的是 **ten-region contract-safe execution boundary**：先跑 producers，再看 readiness，最后才允许 ledger；不要再手写十区 region list，也不要绕过 semantic reload / readiness diagnostics 直接赌 wide ledger。