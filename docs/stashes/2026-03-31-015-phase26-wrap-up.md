# Phase 2.6 阶段收尾总结

**Date:** 2026-03-31
**Branch:** `refactor/loader-reference-grid-alignment`
**Commit Range:** `unknown`
**Status:** Phase 2.6 核心统计、区域图与卫星底图双阶段流程已完成，可作为当前阶段收尾基线。

---

## Key Changes

| File | Change |
|------|--------|
| `src/WA/comparison/phase26.py` | 基于 0.25° 全局缓存计算 `mean/std/participant_count`，并切换到 `std` 口径 |
| `scripts/run_phase2_6_analysis.py` | 新增 Phase 2.6 统计量主入口，输出 `metrics.nc` / `stack.nc` / `summary.json` |
| `docs/phase2_6_metrics_explained.md` | 新增中文说明文档，解释 Phase 2.6 统计量及其含义 |
| `src/WA/visualization/phase26.py` | 新增三联图、区域 2×3 对比图、经纬度刻度控制、区域卫星底图面板 |
| `scripts/plot_phase2_6_metrics.py` | 新增三行一列指标图绘制脚本 |
| `scripts/plot_phase2_6_regional_panels.py` | 新增区域对比图脚本；现改为读取本地卫星底图而非使用 Mean 作为子图 1 |
| `src/WA/phase26_region_imagery.py` | 新增区域 MODIS RGB quicklook 下载逻辑，并支持 400 自动降尺寸回退 |
| `scripts/download_phase2_6_region_imagery.py` | 新增区域底图独立下载脚本，形成“先下载、后绘图”的两阶段流程 |
| `tests/test_phase2_6_analysis.py` | 覆盖 GLWD landmask、`std` 口径切换、参与数统计等分析逻辑 |
| `tests/test_phase2_6_plotting.py` | 覆盖三联图绘制与 participant count 离散色带 |
| `tests/test_phase2_6_regional_panels.py` | 覆盖区域图绘制、经纬度刻度与本地卫星底图接线 |
| `tests/test_phase2_6_region_imagery.py` | 覆盖区域底图下载、manifest 输出、HTTP 400 降尺寸回退 |

## Verification

- pytest: `337 passed`
- ruff: `clean`
- HPC:
  - `cartopy` coastline 缓存已预下载，区域图不再被首次在线下载阻塞
  - 区域 MODIS 底图下载流程已跑通
  - 单个 region 的 `HTTP 400 Bad Request` 已通过自动降尺寸回退修复

## Open Risks / TODOs

- 区域图绘制现在依赖先执行 `download_phase2_6_region_imagery.py`
- GEE 下载仍依赖 HPC 节点上可用的 Earth Engine 凭证与项目权限
- 若未来遇到更极端的大范围区域，`384` 尺寸以下仍可能需要进一步降级策略

## Next Steps

1. 以 `results/phase2.6/` 和 `results/figures/phase2.6_regions/` 作为当前 Phase 2.6 收尾产出基线
2. 进入下一阶段前，如需回看本阶段实现，优先参考本 stash 及 `2026-03-31-008` 到 `2026-03-31-014` 系列 stash
