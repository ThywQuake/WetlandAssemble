# 2026-04-07-001 M001 S01 T01 Inventory Freeze

## Summary

- 已创建 canonical inventory：`.gsd/milestones/M001/slices/S01/S01-INVENTORY.md`
- 冻结了当前本地证据面：`src/WA`、`scripts/`、`tests/`、`docs/`、`config/datasets.yaml`、`todos/`、git/worktree 状态，以及 `results/` / `temp/` / external memory 的本地缺失事实。
- 该 inventory 明确区分了 `present-local`、`current-signal`、`historical`、`superseded`、`absent-local`、`external/HPC-only` 等标签，供后续 S02/S03 直接引用。

## Key Facts Frozen

- `src/WA`: 56 Python source files
- `scripts/`: 54 files
- `tests/`: 71 files; `python -m pytest --collect-only -q` collected 418 tests
- `docs/plans/`: 20 files
- `docs/stashes/`: 147 files
- `docs/datasets/`: 9 files
- `todos/`: 6 files
- `results/`: absent-local
- `temp/`: absent-local
- `../../.claude/projects/-Users-mac-Code-WA/memory`: absent-local from this worktree
- branch/worktree snapshot: `milestone/M001` at `a8680b9`

## Verification Snapshot

- `test -s .gsd/milestones/M001/slices/S01/S01-INVENTORY.md` ✅
- Section heading regex check ✅
- Command / `results/` / `temp/` evidence regex check ✅

## Open Risks

- 本地 inventory 只能证明当前 worktree 可见的证据面；不能把 stash/changelog 当成 HPC 远端状态证明。
- `results/`、`temp/`、以及外部 memory path 在当前 worktree 不可见，后续切片必须把它们视为 proof boundary，而不是“未曾存在”。
- Phase 4 旧 reducer 路径仍保留在仓库里；T01 只做 inventory，不做 canonical-route judgment。

## Next Useful Entry

- 先读：`.gsd/milestones/M001/slices/S01/S01-INVENTORY.md`
- 再做：S01/T02，把 drift / proof boundary / re-entry note 单独整理出来
